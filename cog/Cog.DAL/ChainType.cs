namespace Cog.DAL
{
    public enum ChainType
    {
        /// <summary>Chain all WHERE clauses with AND operator.</summary>
        AND,

        /// <summary>Chain all WHERE clauses with OR operator.</summary>
        OR
    }
}